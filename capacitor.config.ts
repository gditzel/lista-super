import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.listasuper.app',
  appName: 'Lista Super',
  webDir: 'dist/lista-super/browser'
};

export default config;
